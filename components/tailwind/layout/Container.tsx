import React from "react";
// https://tw-elements.com/learn/te-foundations/tailwind-css/containers/
export default function Container() {
  return (
    <>
      <h3>
        Container A component for fixing an element's width to the current
        breakpoint.
      </h3>

      <div className="container bg-purple-300 m-2 p-2 w-1/2 mx-auto">
        <p>
          In the given scenario it's probably better to use an array of selected
          value of anYou can get the array result by using an (empty) array as
          the default value, and registering the chec kboxes under the same
          name. If any values are present inside the array, they will be checked
          by default. The values used in the array will match the checkbox value
          property.
        </p>
      </div>
      <div className=" bg-purple-300 m-2 p-2 w-1/2 mx-auto top-5">
        <p>
          In the given scenario it's probably better to use an array of selected
          value of anYou can get the array result by using an (empty) array as
          the default value, and registering the chec kboxes under the same
          name. If any values are present inside the array, they will be checked
          by default. The values used in the array will match the checkbox value
          property.
        </p>
      </div>
    </>
  );
}
